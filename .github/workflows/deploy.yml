name: Deploy Backend to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ secrets.PORT }}
        script: |
          echo "Starting deployment..."
          
          cd /var/www/ecommerce-backend
          
          cat > deploy-script.sh << 'EOF'
          #!/bin/bash
          set -e
          
          PROJECT_DIR="/var/www/ecommerce-backend"
          BACKUP_DIR="/var/www/backups"
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          
          mkdir -p $BACKUP_DIR
          cd $PROJECT_DIR
          
          # Backup solo si hay datos importantes
          echo "Creating database backup..."
          sudo -u postgres pg_dump ecommerce_import > $BACKUP_DIR/backup_$TIMESTAMP.sql
          
          # Git sync forzado
          echo "Syncing with repository..."
          git fetch origin main
          git reset --hard origin/main
          git clean -fdx
          
          # Install dependencies
          echo "Installing dependencies..."
          COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader --no-interaction
          
          # Clear caches
          echo "Clearing caches..."
          php artisan config:clear
          php artisan cache:clear
          php artisan route:clear
          php artisan view:clear
          
          # Solo ejecutar migraciones si hay pendientes
          echo "Checking migrations..."
          if php artisan migrate:status | grep -q "No"; then
            echo "Running pending migrations..."
            php artisan migrate --force
          else
            echo "No pending migrations"
          fi
          
          # Set permissions
          chown -R www-data:www-data $PROJECT_DIR
          chmod -R 755 $PROJECT_DIR
          chmod -R 775 storage bootstrap/cache
          
          # Restart services
          systemctl restart php8.3-fpm
          systemctl reload nginx
          
          echo "Deployment completed!"
          
          # Health check mejorado
          echo "Performing health check..."
          sleep 5
          
          # Intentar diferentes endpoints para verificar
          if curl -f http://localhost/ > /dev/null 2>&1; then
            echo "Application is responding - deployment successful!"
          elif curl -f http://localhost/admin > /dev/null 2>&1; then
            echo "Admin panel responding - deployment successful!"
          else
            echo "Warning: Application might not be responding correctly"
            echo "Check logs manually: tail -f storage/logs/laravel.log"
            # No hacer rollback automático por los errores de duplicación
          fi
          
          # Limpiar backups antiguos
          find $BACKUP_DIR -name "backup_*.sql" -mtime +7 -delete
          
          echo "Deployment process completed!"
          EOF
          
          chmod +x deploy-script.sh
          ./deploy-script.sh